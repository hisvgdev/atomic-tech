import React, { FC } from 'react';
import { DevWorkLayoutProps } from './DevWorkLayout.types';
import DevWorkWrapper from '../molecules/DevWorkWrapper';

export const DevWorkLayout: FC<DevWorkLayoutProps> = (props) => {
   const {} = props;
   return <DevWorkWrapper />;
};
